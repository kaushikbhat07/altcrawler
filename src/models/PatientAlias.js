export class PatientAlias {
    constructor(alias, assignAuth, aliasType, OID) {
        this.alias = alias;
        this.assignAuth = assignAuth;
        this.aliasType = aliasType;
        this.OID = OID;
    }
}
